name: Retired Apple Review Prep

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired workflow safeguard
    runs-on: ubuntu-latest
    steps:
      - name: Do not mutate App Store review
        run: |
          echo "This legacy build-20 App Review preparation workflow is retired."
          echo "Use the iOS TestFlight Upload workflow for the current build and current Full Quarry Intelligence subscription."
          echo "No App Store Connect changes were made."
