import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ssrockholdingmarketplace',
  appName: 'S&S Rock Holdings',
  webDir: 'dist'
};

export default config;